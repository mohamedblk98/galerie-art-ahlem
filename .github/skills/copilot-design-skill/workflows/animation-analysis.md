# Animation Analysis Workflow

For each interaction:

Trigger -> intent -> initial state -> transition -> duration -> easing -> final state -> reduced motion fallback.

Ask whether animation improves understanding or feedback. If not, remove it.
